// renderer.js: cleaned up. Database operations moved to main process and exposed
// via `preload.js` as `window.electronAPI.saveInvoice()` and
// `window.electronAPI.loadInvoices()`.

// If you need renderer-specific helpers, add them here.